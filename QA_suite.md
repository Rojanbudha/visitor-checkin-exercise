| Test ID | Test Cases | Pre-condition | Steps To Produce | Expected Results | Status |
| --- | --- | --- | --- | --- | --- |
| TC-01 | Verify a visitor can be registered by filling all fields. | At least one host exists. | 1. Fill in a full name, company name, select a host, and enter a purpose. 2. Submit the form. 3. Check the new visitor appears in the active visitors list. | A visitor should appear in the active visitor list with correct attributes. | pass |
| TC-02 | Verify a visitor can be registered without a company or purpose. | App is running; at least one host exists. | 1. Fill in only full name and select the host, leave company and purpose blank. 2. Submit the form. | A visitor should be registered since company and purpose are not explicitly marked as required. | pass |
| TC-03 | Verify the form cannot be submitted when Full name is empty. | App is running. | 1. Leave full name empty and fill all remaining fields. 2. Attempt to submit. 3. Check that the browser shows a validation prompt. | Validation message should appear as "please fill out this field". No visitor is created. | pass |
| TC-04 | Verify the form cannot be submitted when host is not selected. | App is running. | 1. Don't select a host and fill all remaining fields. 2. Attempt to submit. 3. Check that the browser shows a validation message. | Validation message should appear as "please select an item in the list". No visitor is created. | pass |
| TC-05 | Verify that the API rejects an empty registration payload. | API server is running. | 1. Send (POST api/visitors) with an empty JSON body {} directly via curl. 2. Check the response status code and body. 3. Check (GET api/visitors) for a newly created blank record. | Request should be rejected with validation error. | fail |
| TC-06 | Verify a checked-in visitor appears in the Active Visitors list. | A visitor has just been registered. | 1. Register a new visitor. 2. Check the active visitor list for their name. | The visitor appears in the list. | pass |
| TC-07 | Verify a checked-out visitor no longer appears in the Active Visitors list. | An active visitor exists. | 1. Click "Check Out" for a visitor. 2. Refresh the list. 3. Check that the visitor is no longer present. | The visitor disappears from the active list. | pass |
| TC-08 | Verify pagination shows exactly 20 records per full page. | More than 20 visitors exist. | 1. Load page 1 of the Active Visitors list. 2. Count the rows. | Exactly 20 rows are shown. | pass |
| TC-09 | Verify the last page shows the correct remaining visitors. | Active visitors are not an exact multiple of 20. | 1. Navigate to the final page. 2. Count the rows. 3. Check that the "Next" button is disabled. | The final page shows only the remaining visitors and "Next" is disabled. | pass |
| TC-10 | Verify that a deactivated visitor still appears in the Active Visitors list. | A visitor exists who has not checked out. | 1. Deactivate the visitor via PATCH /api/visitors/:id/deactivate. 2. Refresh the Active Visitors list. 3. Check whether the visitor is still listed. | Visitor should not appear in the active list. | fail |
| TC-11 | Verify that a deactivated visitor is still returned by search. | A visitor has been deactivated. | 1. Call GET /api/visitors/search?q=<name>. 2. Check whether the deactivated visitor is included in search results. | Deactivated visitor should not be selectable for a repeat visit. | fail |
| TC-12 | Verify a checked-out visitor can be re-registered. | A visitor has checked out previously. | 1. Check out a visitor. 2. Register a new visit using the same name. 3. Check that registration succeeds. | A person should be able to re-register. | pass |
| TC-13 | Verify that check-in times display in Kathmandu local time, not UTC. | A visitor's raw checked_in_at value is known. | 1. Note the visitor's raw UTC checked_in_at value via GET /api/visitors. 2. Calculate the expected Kathmandu local time (UTC value + 5 hours 45 minutes). 3. Check the time displayed for that visitor in the Active Visitors table. | Displayed time matches Kathmandu local time. | fail |
| TC-14 | Verify display of a check-in that falls just before local midnight. | A visitor's check-in time is set to 23:30 Kathmandu time. | 1. Check the displayed time and date for a visitor. | Should show ~23:30 on the correct local date. | fail |
| TC-15 | Verify display of a check-in that falls just after local midnight. | A visitor's check-in time is set to 01:05 Kathmandu time. | 1. Check the displayed time and (if shown) date for this visitor. | Should show ~01:05 on the correct local date. | fail |
